const validator = require('validator');





const fs = require('fs');
const sum = require("./utils.js")

console.log(validator.isEmail('andrew@exampl.com'));

fs.writeFileSync("notes.txt","???????????????????");